import { Box } from "@mui/material";
import { styled } from "@mui/system";

export const TransactionChartWrapper = styled(Box)({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  width: "600px",
});
